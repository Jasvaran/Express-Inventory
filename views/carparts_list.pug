extends layout 

block content 
    h1 #{title}

    ul 
        each part in carparts_list 
            li
                a(href=part.url) #{part.name}
                |   (#{part.make.year} #{part.make.model})
        else 
            li There are no books