extends layout 

block content 
    h1(style='margin-top:20px')= title 

    div(style='margin-top:20px')

    ul 
        each make in makes 
            li 
                a(href=make.url) #{make.name} #{make.year} #{make.model}
                